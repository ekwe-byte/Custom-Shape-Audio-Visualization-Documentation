# 2.3 - Speed Config

`Bar Moving Speed`: Controls how fast the bars slide along the shape's path.

`Reverse Bar Moving`: Reverses the direction of the bars' movement along the path.

`Shape Rotation Speed`: Rotates the entire shape structure around its center.

`Reverse Shape Rotation`: Reverses the direction of the whole shape's rotation.
